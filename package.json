{
  "name": "abc-map",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "./node_modules/.bin/electron ./api/build/electron-main.js",
    "build": "npm run build-api && npm run build-gui",
    "build-api": "cd api && npm run build",
    "build-gui": "cd gui && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^4.1.3"
  }
}
