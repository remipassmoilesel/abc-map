{
  "name": "@abc-map/infrastructure",
  "version": "1.0.0",
  "private": true,
  "license": "AGPL-3.0-or-later",
  "scripts": {
    "lint": "prettier --log-level warn --check . && sort-package-json --check && pnpm run lint:chart",
    "lint:chart": "helm lint helm-chart/abc-map --strict",
    "lint:fix": "prettier --log-level warn --write . && sort-package-json"
  },
  "devDependencies": {
    "prettier": "3.8.1",
    "sort-package-json": "^3.6.1"
  }
}
